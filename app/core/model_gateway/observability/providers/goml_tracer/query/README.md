# goML query API

`GoMLQueryAPI`: `list_traces()`, `get_span_tree(trace_id)`, `stats()`, `recent_errors()`.

Equivalent to internal HTTP routes in the architecture spec; exposed as Python functions for library consumers.
